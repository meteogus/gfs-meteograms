name: Run and Publish Meteograms

on:
  workflow_dispatch:
  repository_dispatch:

permissions:
  contents: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:

      # ============================================
      # 1. CHECKOUT REPOSITORY
      # ============================================

      - name: Checkout repository
        uses: actions/checkout@v4


      # ============================================
      # 2. SET UP PYTHON
      # ============================================

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"


      # ============================================
      # 3. INSTALL DEPENDENCIES
      # ============================================

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt


      # ============================================
      # 4. DETERMINE GFS RUN HOUR
      # ============================================

      - name: Determine GFS run hour
        id: gfs_run
        run: |

          hour=$(date -u +"%H")

          if ((hour >= 0 && hour < 6)); then
              run_hour="00"
          elif ((hour >= 6 && hour < 12)); then
              run_hour="06"
          elif ((hour >= 12 && hour < 18)); then
              run_hour="12"
          else
              run_hour="18"
          fi

          echo "🕒 Current UTC hour: $hour"
          echo "📦 Using GFS run hour: $run_hour"

          echo "run_hour=$run_hour" >> "$GITHUB_ENV"


      # ============================================
      # 5. RUN METEOGRAM SCRIPTS
      # ============================================

      - name: Run meteogram scripts
        run: |

          python athens.py
          python kiato.py
          python lamia.py
          python patra.py
          python salonica.py
          python agrinio.py
          python chania.py
          python florina.py
          python heraklion.py
          python ikaria.py
          python ioannina.py
          python kefalonia.py
          python larisa.py
          python trikala.py
          python london.py

        env:
          RUN_HOUR: ${{ env.run_hour }}


      # ============================================
      # 6. CREATE LATEST FILES
      # ============================================

      - name: Update latest meteogram files
        run: |

          echo "========================================"
          echo "UPDATING LATEST METEOGRAM FILES"
          echo "========================================"

          cp "athens${run_hour}.png" athens_latest.png
          cp "kiato${run_hour}.png" kiato_latest.png
          cp "lamia${run_hour}.png" lamia_latest.png
          cp "patra${run_hour}.png" patra_latest.png
          cp "salonica${run_hour}.png" salonica_latest.png

          echo ""
          echo "Latest files:"
          ls -lh \
            athens_latest.png \
            kiato_latest.png \
            lamia_latest.png \
            patra_latest.png \
            salonica_latest.png


      # ============================================
      # 7. CHECK GENERATED FILES
      # ============================================

      - name: Check generated PNG files
        run: |

          echo "========================================"
          echo "GENERATED METEOGRAM FILES"
          echo "========================================"

          ls -lh *.png

          echo ""
          echo "Total PNG files:"
          find . -maxdepth 1 -name "*.png" | wc -l


      # ============================================
      # 8. CONFIGURE GIT
      # ============================================

      - name: Configure Git
        run: |

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"


      # ============================================
      # 9. ADD PNG FILES
      # ============================================

      - name: Add generated PNG files
        run: |

          git add "*.png"

          echo "========================================"
          echo "GIT STATUS"
          echo "========================================"

          git status --short


      # ============================================
      # 10. COMMIT CHANGES
      # ============================================

      - name: Commit meteograms
        run: |

          if git diff --cached --quiet; then

              echo "ℹ️ No changes to commit."

          else

              git commit -m "Update GFS meteograms - ${{ env.run_hour }}Z"

          fi


      # ============================================
      # 11. PUSH TO GITHUB
      # ============================================

      - name: Push meteograms to GitHub
        run: |

          git push origin HEAD:${{ github.ref_name }}


      # ============================================
      # 12. SUMMARY
      # ============================================

      - name: Publish summary
        run: |

          echo "========================================"
          echo "✅ GFS METEOGRAM PUBLICATION COMPLETE"
          echo "========================================"

          echo ""
          echo "GFS run: ${{ env.run_hour }}Z"

          echo ""
          echo "Latest URLs:"
          echo "https://meteogus.github.io/gfs-meteograms/athens_latest.png"
          echo "https://meteogus.github.io/gfs-meteograms/kiato_latest.png"
          echo "https://meteogus.github.io/gfs-meteograms/lamia_latest.png"
          echo "https://meteogus.github.io/gfs-meteograms/patra_latest.png"
          echo "https://meteogus.github.io/gfs-meteograms/salonica_latest.png"

          echo ""
          echo "GitHub Pages:"
          echo "https://meteogus.github.io/gfs-meteograms/"
